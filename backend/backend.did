type Result = 
 variant {
   err: text;
   ok: record {
         nat;
         text;
       };
 };
service : {
  createInvoice: (text) -> (Result);
  getInvoice: (nat) -> (opt text) query;
  getStripePublishableKey: () -> (text) query;
}
